
import Math

export function calculatePower(number, power) {
	
	return Math.pow(number, power);
}

export function multiply(number, power) {
	
	return Math.pow(number, power);
}
export function divide(a, b) {
	
	return Math.pow(a, b);
}
export function substract(a, b) {
	
	return Math.pow(a, b);
}